<!DOCTYPE html>
<html>
  <head>
    <title>RailsPortfolio</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>



    <!-- basic fontawesome -->
    <script src="https://kit.fontawesome.com/53f6b212c3.js" crossorigin="anonymous"></script>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <!-- <meta name="viewport" content="width=device-width"> -->

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light" id='index-page-top'>
        <a class="white navbar-brand" href="/">TechBoard</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <!-- <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="white nav-link" href="#">ログイン</a>
            </li>
            <li class="nav-item active">
              <a class="white nav-link" href="#">会員登録</a>
            </li>
          </ul> -->
          <ul class="navbar-nav">
          <% if user_signed_in? %>
                <% if user_admin? %>
                <li class="nav-item active">
                  <%= link_to '管理者ツール', "/admin", class: 'white nav-link' %>
                </li>
                <% end %>
                <li class="nav-item active">
                  <%= link_to 'マイページ', user_path(current_user), class: 'white nav-link' %>
                </li>
                <li class="nav-item active">
                  <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'white nav-link' %>
                </li>
            <% else %>
                <li class="nav-item active">
                  <%= link_to 'サインアップ', new_user_registration_path, class: 'white nav-link' %>
                </li>
                <li class="nav-item active">
                  <%= link_to 'ログイン', new_user_session_path, class: 'white nav-link' %>
                </li>
            <% end %>
            </ul>
        </div>
      </nav>
    </header>

    <main>
      <!-- ログインのflashを設定したが、これは以下で描画できるから不要である -->
      <!-- <div class="login-notification">
        <p class="notice"><notice </p>
        <p class="alert">alert</p>
      </div> -->
      <div class="container">
        <% if flash.notice.present? %>
          <div class="alert alert-success">
            <%= flash.notice %>
          </div>
        <% end %>
      </div>

      <%= yield %>

    </main>

    <footer>
      <div class="footer-nav">
        <p>Top</p>
        <p>Contact</p>
        <p>©️MasatoNumata</p>
      </div>
    </footer>
  </body>
</html>
